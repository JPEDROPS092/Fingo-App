# Arquivo de configuração para desenvolvimento local
# Use este arquivo se você preferir rodar sem Docker

# Dependências do Python (backend)
# Execute: pip install -r backend/requirements.txt

# Dependências do Node.js (frontend)  
# Execute: cd frontend && pnpm install

# Banco de dados local
# SQLite será usado por padrão

# Variáveis de ambiente para desenvolvimento local
export DEBUG=1
export DJANGO_SETTINGS_MODULE=finance_project.settings
export PYTHONPATH="./backend"

# Para rodar localmente:
# Backend: cd backend && python manage.py runserver
# Frontend: cd frontend && pnpm dev
